# @zervi/dxf-parser

DXF/DWG parsing and normalization for the Zervi Pattern Platform.

## Responsibilities

- Parse DXF files into normalized entities
- Extract layers, geometry, text, and blocks
- Validate against the Zervi DXF Template
- Export JSON representations for downstream agents
